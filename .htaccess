<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Serve static files directly
    RewriteCond %{REQUEST_FILENAME} -f
    RewriteRule ^ - [L]

    # Fallback semua route ke index.html (React SPA)
    RewriteRule ^ index.html [L]
</IfModule>
